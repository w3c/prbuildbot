[Travis]
# Make sure you use the correct config URL, the .org and .com
# have different keys!
# TRAVIS_DOMAIN = travis-ci.com
TRAVIS_DOMAIN = travis-ci.org
COMMENT_ENV_VAR = COMMENT_BOT

[GitHub]
GH_TOKEN = <gh-user-token>
ORG = <gh-org>
REPO = <gh-repo>
